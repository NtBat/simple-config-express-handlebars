<h1>Hello</h1>
<p>Paragraph</p>
<p>Nome do usuário {{user.name}} {{user.surname}}</p>

<p>
  {{#if auth}}
    <a href="/logout">Logout</a>
  {{else}}
    <a href="/login">Login</a>
  {{/if}}
</p>

<h1>Veja os posta</h1>

{{#each posts}}
  {{> post}}
{{/each}}